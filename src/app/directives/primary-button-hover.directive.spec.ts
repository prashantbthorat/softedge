import { PrimaryButtonHoverDirective } from './primary-button-hover.directive';

describe('PrimaryButtonHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryButtonHoverDirective();
    expect(directive).toBeTruthy();
  });
});
